# 网络排查与修复（3.8.1）

打开「诊断与工具」，点击「排查网络」。程序会读取系统代理、三个用户代理变量、已配置本地端口和旧会话身份；当前入口在监听时，会对 GitHub 与 OpenAI API 发出不带账号凭据的固定 HEAD 请求。普通打开和自动刷新不执行修复，也不发出这些站点探测。

结果会区分：入口未监听、系统与变量不一致、旧会话遗留、会话身份未知、网关控制未就绪、出口暂停，以及目标 HTTPS 响应。HTTP 403 等错误响应会明确显示；收到 HTTP 响应不等于账号登录或持续对话成功。

点击「修复可处理问题」会先展示具体影响。当前支持：

- **收尾旧会话**：原 owner 已确认退出，才恢复仍属于该会话的设置、核验并归档。保留用户或其他工具后来改动的字段；未知身份不会被当作已退出。
- **对齐用户代理变量**：没有活动或遗留托管会话、当前已配置的 HTTP 系统入口可监听且请求预检成功时，备份后对齐用户变量。保留系统入口、原线路选择和程序分流规则。自定义 NO_PROXY 保留并补充本机绕过。

修复之前会重新核对完整配置和会话快照，配置发生变化则要求重新排查。修复后再次检查，仍需处理的项目继续列出。修复完成但后续观察失败时，明确提示刷新失败，不把已完成的操作改报为失败。

上游未启动、全部线路不可用或外部工具控制网络时，需要在代理管理检测、启动或选择线路；本功能不会自动结束浏览器、Codex 或游戏，不关闭其他工具的 TUN/守卫，不更改 DNS、WinHTTP、订阅或账号。

已经运行的软件可能保留旧环境。修复后仍使用旧入口时，请保存工作并完整重开宿主应用。流向不会声称已修改其他进程的内部环境。

原来的「导出诊断报告」现在也包含最近一次网络排查的时间、问题代码和站点检测状态；不导出修复快照、进程路径或凭据。它是带时间的历史观察，不能代替最新排查。

命令行只读入口：`FlowSwitch.exe --network-diagnostic`，可加 `--data-directory <目录>`；源码入口为 `ProxySwitch.ps1 -NetworkDiagnostic`。修复通过界面预览后的明确操作执行。

同时修复了新 UI 退出漏掉已失效旧会话的问题，以及登录恢复遇到临时错误时没有有限重试的问题。登录恢复不启动网关、不接管第三方入口；三次失败保留会话并记录结果，随后可从界面排查处理。

验证包含 Test-NetworkDiagnostics 的隔离状态/事务/退出/登录重试场景、真实界面按钮与最小窗口宽度，以及编译程序包的诊断入口。真实重启和长期使用仍需实机验收。
