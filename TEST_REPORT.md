# Windows 3.9.0 游戏登录排查验收 · 2026-09-24

本轮源码、隔离测试和程序包分层验证；没有把端口连接或合成测试当作真实游戏登录结果。

- `Test-All.ps1`：完整静态与单元回归通过。首轮与并行测试争用全局 Windows 切换互斥锁后中断，串行重跑通过，未放宽生产锁。
- `Test-ProgramFamilyRouting.ps1`：45 项通过，覆盖进程重用、文件变化、预览过期、冲突保留和并发启动记录不丢失。
- `Test-ProgramCleanStart.ps1`：52 项通过，含真实临时父子进程环境继承、预览身份与 PAC 指纹、过期、重复提交、CAS 恢复和日志失败。新增原入口停止/未知、检测期间外部修改、PAC 保留及 IPv4/IPv6 地址族边界。
- `Test-CleanStartWorker.ps1`：24 项通过；使用真实 Monitor/Child/Guard 和目标 EXE，验证到期、监护崩溃/挂起、锁住恢复标记、损坏启动日志，以及启动发生后的记录故障。失效入口不会被重新启用，迟到的 Monitor 不能重复恢复已完成的会话。Windows 与 RunOnce 写入仅使用临时桩，未改真实用户网络。
- `Test-ApplicationObservation.ps1` / `Test-ManagedObservation.ps1`：52 / 29 项通过，真实入口与直连出口分开，未知回环不猜测；TCP 证据不证明登录。
- `Test-ManagedSwitchChain.ps1`：15 项真实内核、HTTP 和父子程序检查通过。`Test-ProgramIngressIntegration.cjs`：46 项通过，含 A/B/Direct、网站例外、备用恢复及最后的进程、监听和锁清理；进程自然退出码 0。清理完成前不再打印总体通过。
- `Test-RoutingWorkbenchUI.ps1`：49 项真实 WinForms 控件检查；`Test-ObservationUI.ps1`：29 项。新菜单预览、拒绝未修复路径、确认/取消和最小窗口按钮边界通过。`Test-SwitchInteraction.ps1` 通过。
- `Test-VisualTheme.ps1`：100%、125%、150% 布局模拟通过，未改变系统 DPI。
- 快捷方式改用独立 Unicode Shell 接口，`Test-Preferences.ps1` 25、`Test-ProgramLaunch.ps1` 31、`Test-RuleMaintenance.ps1` 35 项原回归通过。`Test-DesktopBranding.ps1` 源码检查 30 项通过，包含中文与非 BMP 名称、参数和图标路径读回、环境变量目标展开、既有 AppID 保留及第三方入口不误加品牌。英文 Windows 云端重验用于确认跨系统编码兼容性。
- 从 ZIP 解压的程序包执行 `Test-WindowsPackage.ps1`，覆盖文件清单、Unicode/空格换目录、实际 EXE 窗口冒烟和隔离桌面快捷方式；成品品牌检查另核对 EXE/窗口图标和稳定 AppUserModelID。最终附件附带 SHA256SUMS 和构建来源，云端发布要求这些检查通过。

本机正常退出旧版后通过更新的桌面入口启动，窗口版本核对为 3.9.0；系统代理保持原样，用户已确认实际任务栏显示新图标。真实游戏大厅、新版长期使用、外部软件持续改写代理和实际 UAC 仍需在目标电脑验收。所有公开截图使用演示数据；私人配置、真实连接记录、恢复快照没有进入发布清单。旧版不删除，运行依赖版本未变。

---
# Windows 3.8.4 双入口图标与冰蓝主题验收 · 2026-09-19

运行代码来源 `5acef692a267e8c5b5919a02026b74a7168a5ced`。Windows 运行文件与该提交一致，后续仅补充文档与下载入口。代理后端和运行依赖未修改。

- `Test-All.ps1`：完整静态与单元回归通过。
- `Test-VisualTheme.ps1`：100%、125%、150% 布局模拟通过，包含 120 行、三页导航、三种宽度、80 次拖动缩放、滚动和裁切检查。未修改原 20 秒门槛。首轮总耗时超出门槛，旧版对照也超时；分段检查约 12.36 秒，最终原始测试三种缩放全部通过。缩放是布局模拟，不是系统 DPI 实测。
- `Test-RoutingWorkbenchUI.ps1`：35 项真实控件检查通过，包含网站规则编辑、取消与诊断分发。
- `Test-SwitchInteraction.ps1`：切换优先级、统一入口分发、模拟系统写入和进度检查通过。
- 实际窗口验证双入口图标、深蓝灰与冰蓝主题。修正小尺寸 PNG 压缩 ICO 被旧版 .NET 绘制为杂点的问题：16–128 像素使用兼容位图，256 像素保留 PNG；原图透明度保留。
- 最终 ZIP 解压后运行 `Test-WindowsPackage.ps1`：15 项通过，包含清单、中文/空格搬移、EXE 窗口冒烟、版本、隔离数据和快捷方式。没有写真实网络设置。

本轮不等同真实账号登录或长期网络稳定性验收。历史记录保留如下。

---
# Windows 3.8.3 界面改版验收 · 2026-09-19

运行代码对应提交 `2d18e1a0a4592324c1b72cf67f8548a1dc1cbdbd`（本次之后仅更新说明与验收记录）。代理后端没有修改。

- `Test-All.ps1`：全部静态与单元检查通过，包括离线恢复、路由策略、生命周期与数据隔离。
- `Test-VisualTheme.ps1`：100%、125%、150% 布局模拟通过；120 行、三页导航、三种宽度、80 次拖动缩放、滚轮/键盘/拖动滚动条、摘要卡片边界。125%/150% 是布局模拟，不是系统 DPI 变更。
- `Test-RoutingWorkbenchUI.ps1`：35 项真实控件检查通过，包含网站规则编辑、取消和诊断分发。
- `Test-RoutingWorkbenchUI.ps1`：35 项真实控件检查通过，包含网站规则编辑、取消与诊断分发。
- `Test-SwitchInteraction.ps1`：显式切换优先于后台扫描、统一切换操作分发、模拟写入及进度通过。旧测试仍指向 3.0 的系统入口路径，现明确在统一分流入口设置测试替身；真实核心迁移由现有独立测试覆盖，不能将该 UI 测试称为真实网络切换。
- 最终 ZIP 校验 CRC、单应用根、无重复/越界路径和 43 项文件摘要；从 ZIP 解压后再次运行 `Test-WindowsPackage.ps1`，15 项通过，包括中文/空格换目录、EXE 真实窗口冒烟及隔离快捷方式。
- 本地安装完整核对摘要，窗口标题验证为 3.8.3。测试截图全部是演示数据。原配置未重置，没有调用真实网络切换。

程序包：71,288,388 字节；SHA-256 `894b93fac428685a9eb6b4cf8477d8d8853fc895eca2911968f762cb55fdb377`。

这轮界面验证不等同于真实账号登录、代理网站可用性或长期网络稳定性验收。历史网络测试证据如下，不能扩展为本轮全部重新实测。

---

## 3.8.2 失效入口恢复验证（2026-09-16，Windows x64）

- Test-DeadEntryRepair：21 项隔离检查，覆盖配置已移除、无备用清理、变量/规则保留、未知监听、外部守护、并发端口恢复、配置变化和失败回滚。
- Test-DeadEntryIntegration：13 项真实内核与 HTTP 检查，覆盖坏入口接备用 B、独立程序入口和网站例外保留、实际出口核验失败回滚、退出不恢复坏入口。Windows 设置和 RunOnce 写入隔离。
- Test-RoutingWorkbenchUI：35 项实际控件断言。完整静态/单元回归与旧外部引擎离线迁移回归同时执行。
- 自动化检查不修改真实用户网络，不代表真实 IDE OAuth 登录、账号访问或所有应用已接入流向。HTTP 响应、规则加载、登录成功分别判定。

# FlowSwitch 3.8.0 核心重构候选验证

2026-09-12，Windows x64 / Windows PowerShell 5.1，复用已锁定 Node.js 24.19.0 和 mihomo 1.19.29。本轮隔离回归、真实本地内核整链及最终 Windows 程序包验证已通过。候选未覆盖用户日常安装或个人代理配置；跨电脑和真实账号登录仍待使用者验收。

| 实际执行的检查 | 结果与证据范围 |
| --- | --- |
| Test-All.ps1 | 最终完整静态与单元套件通过，含迁移事务 46、程序入口 39、父子关系 35、启动证据 14、观察 25、入口移除 32、域名策略 32、离线分离 39 项，以及全部原有身份/事务/生命周期/锁回归。 |
| Test-ManagedSwitchChain.ps1 | 15 项通过：实际正常选线入口、真实父子程序使用相同稳定地址，在同一 PID 下 A/B/Direct 改线；网站直连与默认代理共存；停止后按原端口和目标启动。 |
| Test-OfflineMigration.ps1 | 36 项通过：旧引擎/控制器/端口均离线，实际 Set-UniversalProxy → 离线分离 → 自有内核 → HTTP B；四文件含 BOM 原字节恢复；外部修改保留；预检 config/state 变化拒绝；普通系统代理开关不阻止明确切换。 |
| Test-SwitchChain.ps1 | 21 项通过：真实后端、核心和 HTTP 验证 A→B→A→Direct→B；固定地址不变，手动返回首选，错误 selector 拒绝并回滚，完整停止后重启。 |
| Test-ProgramIngressIntegration.cjs | 42 项通过：独立程序入口、父子后台流量、域名规则及具体度、选择性重连、有限内核恢复、单个程序端口失败隔离。 |
| Test-GatewayIntegration.cjs | 最终 29 项通过：原外部分流真实客户端 A/B/SOCKS/Direct、PID 与端口保持、规则顺序、故障回滚和外部修改保护。 |
| Test-IndependentIntegration.cjs / Test-Lifecycle.cjs | 27 / 41 项通过：备用接替、失效上游、全部失败暂停、无关修改不回跳，以及真实内核异常重启、次数上限、端口占用和停止竞争。 |
| Test-TrayLifecycle.ps1 / Test-SupervisorRecovery.ps1 | 最终 12 / 11 项通过：真实 NotifyIcon 与隔离内核，关闭驻留、内核重启、显式停止、恢复失败留证及外部新设置保留。 |
| 最终程序包 | Test-WindowsPackage 13、Test-WindowInstance 5、Test-RuntimeBundle 17 项通过：编译 EXE、中文/空格迁移路径、空 PATH、实际 UI 冒烟、同目录单实例、显式数据目录、缺失文件拒绝及自带核心实际 HTTP。 |
| UI 与启动请求 | ObservationUI 29、RoutingWorkbenchUI 32、LaunchDispatch 27 项通过；被动 Standalone 窗口 6 秒、3 轮读取/刷新/退出无配置写入或协议探测。短回归不代表长期观察。 |

Windows 程序包共 43 个文件（含 42 项逐文件 SHA-256 清单），沿用原有运行组件，没有新增依赖下载。构建后再次核对所有运行源文件与包内字节一致。ZIP 大小、SHA-256、CRC、单应用根和源码包清单另见发布目录校验记录。

所有故障注入均使用隔离目录、测试核心及自建 HTTP/父子程序；Windows 代理、用户环境和 RunOnce 写入使用桩。不读取登录链接、Cookie、令牌或第三方进程内存，不开启 TUN，不结束用户应用。

尚未完成：另一台电脑的真实 IDE 打开、浏览器授权返回客户端、客户端换取令牌、登录后请求与长期稳定性。首次接入仍须完整重开原应用；直接缓存旧 A 地址、没有进入流向或忽略代理的流量无法凭保存 B 自动接管。HTTP/HTTPS 响应、控制器状态和隔离测试均不能替代账号登录验收。

---

## 历史记录边界

以下完整保留 3.7.2 及更早版本的测试、路径、包大小、哈希和结论，仅适用于当时版本，不属于 3.8.0 当前通过清单。

# FlowSwitch 3.7.2 候选维护验收

2026-09-11，Windows x64 / Windows PowerShell 5.1。候选在独立工作副本构建，未覆盖当前安装、桌面入口或个人规则；未发布到 GitHub。正式软件可用性及另一台电脑真实登录仍待验收。

## 本轮得到的关键证据

- 显示误报：Windows CIM 多状态查询在某个状态没有记录时抛出 NotFound。只读实测 Listen + DeleteTCB，Stop 模式整轮赋值为 0 条，非中断收集仍有 43 条。原代码会把这种情况当成正常空表。现改为单次读取完整 TCP 表后筛选；真正读取失败仍显示未知。
- 手动重新选择 A：原普通规则同步保留备用 B 的逻辑也作用于手动重选，导致 A 恢复后仍走 B。现在明确手动选择重置首选组，后台刷新及无关规则修改仍保留有效备用。
- 独立入口停止：此前即使已经配置独立分流，点击统一切换也直接因未监听失败。现在显式切换可启动已配置入口，再核验目标和真实请求后提交；不会仅打开窗口就改网络，也不自动迁移外部引擎。
- 出口真假：真实 HTTP 可用但 selector 与所选出口不符时拒绝成功并恢复原线路。默认线路的程序可以打开预览重连菜单，未知／未加载规则仍受保护。
- IDE 启动链：已有 launch 记录改线路不再变为单 EXE 引擎规则。真实临时父子 EXE 验证下一次启动从 A 改为 B 的参数和环境继承；运行中的环境不会被改写。
- 入口与诊断：失效旧快捷方式明确识别、显式修复保留原始备份及外部编辑；后端初始化失败使用短错误码和单个有界本地报告；Google 诊断读当前系统入口。EXE 和 UI 版本由同一变量生成。

## 实际执行的验证

| 验证 | 结果与范围 |
| --- | --- |
| Test-All.ps1 | 完整静态与单元套件通过，505 项；包括并发锁、身份、所有权、代理事务和新的空闲 TCP 状态回归 |
| Test-EntryMaintenance.ps1 最终复测 | 16 项通过；相较上项将启动失败报告收敛为单文件后再验证，重复失败仍只保留一份小于 4 KiB 的固定字段报告 |
| Test-SwitchChain.ps1 | 21 项通过；实际 PowerShell 切换后端 → 真实隔离内核 → 本地 HTTP A/B/D 标记响应；冷启动、A→B→A→Direct→B、手动从备用 B 返回 A、假 selector 拒绝及真实回滚、固定端口／内核不变、状态与请求一致、停止后选择 B 重启 |
| Test-ObservationUI.ps1 | 24 项真实控件检查通过；launch 模式、父子启动菜单、默认线路重连、未知规则禁用及诊断按钮 |
| Test-SwitchInteraction.ps1 | 真正点击统一切换，验证操作优先、进度和模拟 Windows 设置实读一致 |
| Test-GatewayIntegration.cjs | 29 项通过；两个实际测试程序保持 PID、按程序重连、规则顺序、回滚与外部更改保留 |
| Test-IndependentIntegration.cjs | 27 项通过；自动接替、端口存活但上游故障、全部失败暂停、程序隔离、无关规则修改不回跳 |
| Test-Lifecycle.cjs | 41 项通过；真实内核异常恢复、就绪、锁恢复、端口占用与停止竞争 |
| Test-TrayLifecycle / SupervisorRecovery | 12 / 11 项通过；关闭到托盘和监督进程恢复 |
| Test-WindowsPackage.ps1 最终包 | 13 项通过；39 个文件／38 项清单、实际 EXE、版本一致、移动后中文空格路径、真实隔离 UI、缺失文件处理 |
| Test-WindowInstance / RuntimeBundle | 5 / 17 项通过；单实例、清空 PATH、自带内核真实 HTTP、恢复与运行组件兼容；最后单文件启动报告变更不涉及这些成功路径 |

测试只使用临时配置、自己的测试进程及回环端口，Windows 代理、用户环境与 RunOnce 写入均使用桩。没有结束用户 IDE、浏览器，没有开启 TUN，没有读取或记录登录凭据。TCP 缺失状态复现仅做只读聚合查询。

## 测试期间发现并处理的问题

首次界面诊断按钮测试在未选中该页面时 PerformClick 没有触发；切换实际页面后完成真实按钮验收。一次并发运行的旧单元测试遇到全局互斥竞争，最终套件顺序执行通过。首次真实显示断言失败引出了上述多状态 TCP 查询缺陷，修复后完整单元、真实切换链及界面测试再次通过。

首个 Windows 构建被版本一致性测试拦截（EXE 手写 3.7.1、UI 已是 3.7.2），已改为构建时生成 EXE 元数据并从该元数据输出版本。早期 Windows 与 Windows-r2 目录保留为过程产物，交付只使用 Windows-verified。最后将初始化失败报告限制为单文件，并复测 16 项入口测试及最终包 13 项检查。

## 最终候选包

- 路径：Windows-verified/FlowSwitch-v3.7.2-Windows-x64.zip
- 大小：71,477,409 字节
- SHA-256：`f537cb39526d0a1dbea092e99900f119628adad4509e782b5608998ee830ada1`
- ZIP CRC 全部通过；唯一根目录 FlowSwitch；清单中每个文件的大小与 SHA-256 均吻合。使用现有锁定运行组件构建，没有下载依赖。

## 仍待另一台电脑验收

Antigravity IDE 的真实启动、浏览器授权回调返回 IDE、账号登录后实际请求、各条真实上游的 Google HTTPS 可用性，以及长时间稳定性。隔离 HTTP／CONNECT 成功不能代替这些结果。

普通引擎规则按 EXE 匹配，父子启动代理依赖新进程读取环境／参数；任意已有后台服务、外部浏览器或绕过固定入口的流量并未实现透明迁移。前期版本的改动和明日复测步骤见 MAINTENANCE.md。


---

以下为历史版本验收记录：

# FlowSwitch 3.7.1 验收记录

2026-09-11，Windows x64 / Windows PowerShell 5.1。607 项相关检查通过。测试使用隔离文件、回环端口及自有测试进程；网络设置写入使用桩，成品包通过真实窗口验证。

| 验证 | 已执行结果 |
| --- | --- |
| Test-All.ps1 | 481 项：原 470 项及新增 11 项真实 Windows 锁检查；PowerShell 语法解析通过 |
| Test-GatewayLock.cjs（已计入上项） | 空锁、截断 JSON、null、PID 复用可恢复；活跃持有者与仍打开的空文件不可接管；八进程并发恢复不会重叠进入临界区 |
| Test-Lifecycle.cjs | 41 项：空 supervisor 锁下重开、空 mutation 锁下真实接替、截断锁下规则更新、保留备用、有限崩溃恢复和停止竞争 |
| Test-IndependentIntegration.cjs | 27 项：真实核心自动接替、存活但故障的代理、全部失败暂停、按程序隔离及保留备用 |
| Test-TrayLifecycle / SupervisorRecovery | 12 / 11 项：真实托盘和自有进程生命周期，设置恢复使用隔离桩 |
| Test-RuntimeBundle | 17 项：清空 PATH、自带组件、真实转发与退出恢复 |
| Test-WindowsPackage / WindowInstance | 13 / 5 项：39 文件/38 清单项成品包、中文/空格路径、完整 UI、同配置单实例与快捷方式 |

## 修复依据

3.7.0 的损坏锁复现中，A 已退出且 B 健康，等待 12 秒仍不能接替；只删除隔离测试锁后恢复。3.7.1 不按文件年龄直接删除锁，改为在禁止其他写入和删除的文件句柄内核对原内容、PID 与启动时间，再写入新持有者。活跃旧版写入者保持不动，正常无冲突操作不启动额外恢复进程。

并发测试还覆盖多个恢复者争用同一旧锁；释放操作等待短暂占用的恢复句柄，避免失败后遗留仍存活的持有者。停止后重开遇到损坏 singleton 时，通过已有监督进程记录判断旧会话，随后走受控锁恢复。

这些故障注入不等于确认用户历史断网的唯一原因。另一台电脑的硬件兼容性、真实登录、长时间稳定性和性能仍须按 [验收约定](ACCEPTANCE.md) 分别确认。新版本不新增 TUN、自动结束应用、复制订阅或默认全失败直连行为。

---

以下为 3.7.0 的历史验收记录，保留原验证范围：

# FlowSwitch 3.7.0 验收记录

2026-09-11 在 Windows x64 / Windows PowerShell 5.1 上完成。故障注入只使用测试创建的内核、客户端和隔离数据目录；Windows 设置通过桩验证，没有结束用户应用、切换真实网络或读取凭据。

| 验证 | 已执行结果 |
| --- | --- |
| Test-All.ps1 | 470 项断言通过；所有 PowerShell 文件语法解析通过 |
| 其中：身份 / 连接 / 维护 / worker | 42 / 28 / 35 / 7 项；真实文件别名、注册包升级候选、连接未知状态、记录修复与已完成操作结果保留 |
| 其中：统一切换 / 路由器 | ProxySwitch 59、AppRouter 67、IndependentRouter 28 项；规则 / 设置双 hash 校验、外部修改保护与精确进程身份锁 |
| Test-Lifecycle.cjs | 38 项真实内核检查；异常终止、退避、上限、占用端口、启动失败、停止中重开、残留锁恢复与修复路径保持备用 |
| Test-GatewayIntegration.cjs | 29 项真实内核检查；同一客户端改线路、选择性重连、规则顺序、部分 API 失效、回滚及外部配置保留 |
| Test-IndependentIntegration.cjs | 27 项；备用接替、全上游失效、无关规则不回跳、读取部分失败与过期计划拒绝 |
| Test-TrayLifecycle.ps1 | 12 项；真实 NotifyIcon 窗口、X 隐藏、入口保持、恢复失败保留窗口和内核、明确停止 |
| Test-SupervisorRecovery.ps1 | 11 项；崩溃 supervisor、重启后的 child、精确创建时间与不依赖父管道的孤儿内核 |
| Test-ExitRecovery / Watchdog | 16 / 4 项（已计入 Test-All）；环境恢复竞态、旧 watchdog 不操作新会话、排除即将停止的自有入口、有限失败重试 |
| Test-ObservationUI.ps1 | 16 项真实 WinForms 控件；旧记录与新连接、稳定选择、修复/移除目标、未知监听/出口与陈旧数据 |
| 旧界面回归 | DiscoveryUI、AutomaticDiscoveryUI、SwitchInteraction、PassiveLifecycle、VisualTheme 均通过；含 72 秒 12 轮只读观察、120 行、3 种尺寸、80 帧拖动无原生横向滚动条 |
| Test-WindowsPackage.ps1 | 13 项；38 文件完整包、37 项 manifest、EXE/UI 版本一致、中文与空格路径、完整界面启动、缺失文件与桌面入口 |
| Test-RuntimeBundle.ps1 | 17 项；空 PATH、锁定组件、真实内核转发、原组件复用、短时重开和退出恢复 |
| Test-WindowInstance.ps1 | 5 项；完整 EXE 打开正确版本窗口，同配置目录只保留一个窗口和托盘 owner |
| 本机只读程序识别 | 旧商店应用记录关联当前版本，显示实际代理 TCP 连接并标为路径待修复；未运行的保存程序明确显示未运行 |

## 本次复现并修复的问题

旧 MSIX 路径与新版进程被拆成两行；连接读取失败被当成空闲；SynSent 丢失目标端口；父程序误汇总有独立规则的子程序；配置或连接 API 部分失败使全部证据丢失；规则被前置 MATCH 遮蔽仍被算作已加载；成功写入后的界面刷新错误丢失操作结果。

恢复方面，已复现停止中重新启动复用旧 child、恢复环境变量时覆盖外部新值、旧 watchdog 等锁后恢复新会话、自有入口被当作可恢复原代理、残留更新锁阻止接替，以及修复 EXE 路径使共享出口回跳。这些场景均有对应回归。普通规则写入及回滚也在引擎锁内核对状态和配置版本。

正常刷新只进行观察与已授权的代理发现。修复程序路径需要明确核对计划；计划过期、身份有歧义、目标已存在记录或外部修改时拒绝覆盖。原线路和自有快捷方式原始备份保留；外部修改的快捷方式保持原样。

## 验收边界与另一台电脑复测

本机与隔离测试通过不等于另一台电脑的登录验收。原错误只能确认当时本机代理入口不可用，不能倒推出历史内核是正常退出还是崩溃。

请按 [功能验收约定](ACCEPTANCE.md) 在目标电脑验证启动顺序、X 到托盘后继续使用、应用升级后的旧记录修复，以及真实 Google 登录和日常运行。Google OAuth 诊断只发出无凭据请求；CONNECT 200、HTTP 404/401、TCP 连接成功均不代表账号登录成功。程序可能使用已继承的旧代理地址，需由使用者保存工作后完整重开应用或启动器。

未自动结束应用、开启 TUN、复制订阅或收集令牌。核验程序包和公开 SHA-256 见 [下载与校验](https://github.com/turnsolesama/portfolio/blob/main/proxy-switch/releases/README.md)；私有实机日志与备份不随包发布。
