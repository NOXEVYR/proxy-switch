# 游戏登录与直连对照

保存为“直连”表示所选出口，不表示程序绕过了本地代理入口。3.9.0 将“经流向 → 直连”、TUN 入口、已核验的程序内部通信、未确认归属的回环连接和入口外 TCP 分开显示。程序详情列出当前采样的进程、地址、端口和线路；TCP 已建立不等于账号认证成功，未观察到也不等于断网。

## 建议操作顺序

1. 登录失败时先保留现场，在程序详情查看启动器、主程序和登录子进程的实际连接。
2. 为主程序选择明确线路，右键“检查并补齐整组程序规则”。确认预览后仅补充有当前父子关系及文件身份依据的遗漏成员。已有不同线路、网站例外不会被覆盖；不能只因同名或同目录就加入规则。此操作需要规则引擎就绪。
3. 保存工作，正常退出游戏、启动器和托盘进程。修改线路不能清除旧进程继承的环境或旧登录会话。
4. 右键“干净环境启动”：只清理新程序环境块里的代理变量，不改用户环境或系统代理。程序自身保存的代理设置仍可能生效。
5. 仍有问题时，可明确选择“直连登录对照”。它在启动前临时将当前用户 WinINet 设为直连，最长五分钟，期间其他遵循系统代理的应用也受影响。窗口中的确认说明会提示该影响；需要管理员权限的程序须手动勾选并通过正常 UAC。
6. 对照完成后点“诊断与工具 → 结束直连对照”，或等待超时恢复。恢复前核对设置仍属于本次操作，其他软件后来修改过的代理或自动配置会保留并提示。原本地入口已停止时，不重新启用失效的手动代理，保留原自动配置模式；入口状态未知则保留快照，提示重试。恢复失败保留记录；后台监视与登录恢复入口用于异常兜底。

不会自动结束游戏，不会修改 DNS、WinHTTP、路由、TUN 或第三方代理开关。直连对照不是“永久让所有游戏绕过代理”的开关，也不会绕过游戏登录或反作弊机制。

## 如何解读对照结果

完整退出后，通过干净环境及临时直连能够进入大厅，说明这组条件有效；仅此不能确定是代理入口、继承环境还是旧登录会话中的哪一项导致失败。请记录“进入大厅 / 仍报错”和恢复代理后的表现，勿将 HTTP 成功或端口连通代替真实登录验收。

## 任务栏仍显示旧图标

3.9.0 为 EXE、运行窗口和桌面快捷方式写入同一个稳定的双入口品牌标识。完整解压新版后运行“创建桌面快捷方式.cmd”，原快捷方式先备份。已有窗口需正常退出后重新打开；固定在任务栏的旧入口可取消固定后从新版窗口重新固定。程序不会清空全局图标缓存或重启资源管理器。
